# clip-farm dependencies
# Python 3.11+

yt-dlp>=2024.1.0
PyYAML>=6.0
openai>=1.0.0
python-dotenv>=1.0.0

# Audio energy analysis (librosa-based scoring)
librosa>=0.10.0
soundfile>=0.12.0

# Scene detection (PySceneDetect with OpenCV backend)
scenedetect[opencv]>=0.6

# YouTube Data API v3 (trending video discovery + upload)
google-api-python-client>=2.100.0
google-auth-oauthlib>=1.0.0

# Local Whisper via Apple MLX — uses Apple Silicon GPU natively (free, fast)
mlx-whisper>=0.4.0

# Web UI (optional): cd to repo root, then:
#   uvicorn web.backend.main:app --reload --port 8000
fastapi>=0.100.0
uvicorn[standard]>=0.23.0

# FFmpeg must be installed separately (brew install ffmpeg / apt install ffmpeg)
